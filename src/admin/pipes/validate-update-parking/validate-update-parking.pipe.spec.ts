import { ValidateUpdateParkingPipe } from './validate-update-parking.pipe';

describe('ValidateUpdateParkingPipe', () => {
  it('should be defined', () => {
    expect(new ValidateUpdateParkingPipe()).toBeDefined();
  });
});
