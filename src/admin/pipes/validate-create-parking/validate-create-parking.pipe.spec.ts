import { ValidateCreateParkingPipe } from './validate-create-parking.pipe';

describe('ValidateCreateParkingPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateParkingPipe()).toBeDefined();
  });
});
